using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BasicShapeGenerator : MonoBehaviour {

    public Shape shape;

    public static int[,,] sphere;

    public enum Shape {Sphere, Cone, Pyramid, Cube};

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
